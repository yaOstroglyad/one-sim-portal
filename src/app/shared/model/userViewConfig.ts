export interface UserViewConfig {
    /**
     * Primary color of user hex ex: #fffffff
     */
    primaryColor: string;
    /**
     * User's default language
     */
    language: string;
    /**
     * User's default logoName
     */
    logoName: string;
}

